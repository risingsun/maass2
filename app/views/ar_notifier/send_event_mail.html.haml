= "Hi #{@profile.full_name}"

= @event.title
= @event.description
= "Where: #{@event.place}"
= "Start Date: #{@event.start_date.to_datetime.to_formatted_s(:rfc822)}" if @event.start_date
= "End Date: #{@event.end_date.to_datetime.to_formatted_s(:rfc822)}" if @event.end_date
= "Hosted by: #{@event.organizers.first.full_name}"
= render :partial => 'account_mailer/email_footer'