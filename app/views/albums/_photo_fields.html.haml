.fields
  = f.inputs do
    = f.input :image, :label => "Upload your Image (<=5Mb)", :wrapper_html => {:class => cycle(:row_light2, :row_dark2)}
    = f.input :caption, :wrapper_html => {:class => cycle(:row_light2, :row_dark2)}
