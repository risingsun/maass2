#cropwrap
  = image_tag(@photo.url_for_size('original'), :id => "cropbox")
#cropresults
  %br
  = f.input :crop_x, :wrapper_html => { :class => "#{zebra}_complete"}
  = f.input :crop_y, :wrapper_html => { :class => "#{zebra}_complete"}
  = f.input :crop_w, :wrapper_html => { :class => "#{zebra}_complete"}
  = f.input :crop_h, :wrapper_html => { :class => "#{zebra}_complete"}