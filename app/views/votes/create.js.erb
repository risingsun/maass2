<% if @args %>
  jQuery("#<%= @vote.poll_id%>").replaceWith("<%= escape_javascript(render :partial => 'polls/poll_response', :locals => {:poll => @vote.poll})%>");
  <% else %>
  jQuery("#poll_errors").prepend("<div id='poll_messages' class='error_msg'>Please select a option</div>");
<% end %>