%h2
  = @profile.first_name
  's polls
  = link_to image_tag('add.png',:title=>"Create New poll"),new_profile_poll_path(@profile, @polls) if @p == @profile
  -if @polls.empty?
    = @profile.full_name
    has no polls .
  - else
    = render :partial => 'polls/poll', :collection=>@polls
  = will_paginate @polls