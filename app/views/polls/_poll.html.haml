%h4
  = @poll.question
%br
  Posted by
="#{current_user.first_name}"
at
="#{@poll.created_at.to_date.to_formatted_s(:rfc822)}"
= link_to image_tag('cancel.png', :title => "Close poll"),poll_close_poll_path(@poll) unless @poll.poll_close?
= link_to image_tag('edit.png', :title => "Edit poll"),edit_poll_path(@poll)
//= link_to("Delete", @poll,:method => :delete, :confirm => "Are you sure?", :remote=>true)
%br