= rounded_med_corner(:title => "Upload Icon", :id => "upload_photo") do
  = form_for @profile,:html => { :multipart => true } do |f|
    = f.label "Upload your Icon (<=5Mb)"
    = f.file_field :icon, :wrapper_html => {:class => cycle(:row_light2, :row_dark2)}
