.fields
  = f.input :education_from_year, formtastic_zebra(:as => :select, :collection => EDU_YEAR, :prompt => true)
  = f.input :education_to_year, formtastic_zebra(:as => :select, :collection => EDU_YEAR, :prompt => true)
  = f.input :institution, formtastic_zebra
  %div{:class => zebra}
    = link_to_remove_fields(image_tag("remove.png"), f)