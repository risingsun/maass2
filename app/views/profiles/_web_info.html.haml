- rounded_med_corner(:id => "address_details", :title => "Web Info") do
  = semantic_form_for @profile do |f|
    = f.inputs do
      = f.input :website, :as => :string, :wrap_class => cycle(:row_light2, :row_dark2)
      = f.input :blog, :as => :string, :wrap_class => cycle(:row_light2, :row_dark2)
      = f.input :flicker_username,:as => :string, :wrap_class => cycle(:row_light2, :row_dark2)
      = f.input :linkedin_name, :label => "LinkedIn Public URL",:as => :string, :wrap_class => cycle(:row_light2, :row_dark2)
      = f.input :twitter_username, :as => :string, :wrap_class => cycle(:row_light2, :row_dark2)
      = f.input :aim_name, :label => "AIM",:as => :string, :wrap_class => cycle(:row_light2, :row_dark2)
      = f.input :msn_username, :label => "MSN",:as => :string, :wrap_class => cycle(:row_light2, :row_dark2)
      = f.input :yahoo_name,:label => "Yahoo UserId", :as => :string, :wrap_class => cycle(:row_light2, :row_dark2)
      = f.input :gtalk_name, :as => :string, :wrap_class => cycle(:row_light2, :row_dark2)
      = f.input :skype_name, :as => :string, :wrap_class => cycle(:row_light2, :row_dark2)
      = f.input :delicious_name, :label => "delicious name", :as => :string, :wrap_class => cycle(:row_light2, :row_dark2)
    = f.commit_button :label => "Update"


 