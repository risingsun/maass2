%h2.page_title
  = SITE_NAME
  Events
  = link_to image_tag('add_new_post.png',  :title=>"Create New Event",  :class=>"add_new_post2"),  new_admin_event_path()
= render :partial => 'admin/events/event', :collection => @events, :locals => {:size => "large"}
.clear_div

