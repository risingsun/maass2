= form_for @announcement,:url => admin_announcements_path, :html => {:method=> :post} do |form|
  = rounded_corner(:title => "Create Announcement", :size => 'lrg') do
    = render :partial => 'form', :locals => {:f => form}
    .large_btn_container2
      %button.buttons{:type => "submit"}
        = image_tag('save.png')