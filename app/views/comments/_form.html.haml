= form_for(Comment.new) do |c|
  = c.text_area :comment
  = c.hidden_field :commentable_id, :value => commt.id
  = c.hidden_field :commentable_type, :value => commt.class.name
  = c.submit
  %input#cancel{:type => "button", :value => "Cancel", :onclick => "cancel_comment(#{commt.id})"}