#cropwrap
  = image_tag(set_image(photo,'original'), :id => "cropbox")
#cropresults
  %br
  = f.input :crop_x, :label => "X", :wrapper_html => {:class => cycle(:row_complete_light, :row_complete_dark)}
  = f.input :crop_y, :label => "Y", :wrapper_html => { :class => cycle(:row_complete_light, :row_complete_dark)}
  = f.input :crop_w, :label => "Width(=531px)", :wrapper_html => { :class => cycle(:row_complete_light, :row_complete_dark)}
  = f.input :crop_h, :label => "Height(=264px)", :wrapper_html => { :class => cycle(:row_complete_light, :row_complete_dark)}
